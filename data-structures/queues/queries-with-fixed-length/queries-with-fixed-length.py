# https://www.hackerrank.com/challenges/queries-with-fixed-length/problem

import collections 

def solve(arr, queries):
    n = len(arr)
    result = []

    for d in queries:
        q = collections.deque()
        for i in range(n):
            while q and :
                q.

            q.append(arr[i])